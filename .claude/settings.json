{
  "permissions": {
    "allow": [
      "Bash(bun install)",
      "Bash(bun test*)",
      "Bash(bun run dev*)",
      "Bash(bun run typecheck)",
      "Bash(bun run lint*)",
      "Bash(bun run format*)",
      "Bash(bun run drizzle:*)",
      "Bash(bunx tsc --noEmit)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(cat | jq -r '.tool_input.file_path // empty') && if echo \"$FILE\" | grep -q 'packages/database/src/schema'; then echo 'Schema file changed — remember to run: bun run drizzle:generate'; fi"
          }
        ]
      }
    ]
  }
}
